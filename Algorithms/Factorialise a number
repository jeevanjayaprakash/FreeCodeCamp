/**
 * Factorialises a number
 *
 * Jeevan Jayaprakash
 * jeevan.jayaprakash@cantab.net
 *
 * A function which factorialises a number provided by the user
 */
 
 function factorialize(num) {
  var array = [];
  for (var i = num; i > 0; i--) {
    array.push(i);
  }
  var singleVal;
  singleVal = array.reduce(function(previousVal, currentVal) {
  return previousVal * currentVal;
}, 1);
  return singleVal;
}
